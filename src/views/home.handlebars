<h1 class="title">List of products</h1>

{{#if products}}
  <div class="product-container">
    {{#each products}}
      <div class="product">
        <p>Name: <span>{{title}}</span></h2>
        <p>Price: <span>{{price}}</span></p>
        <p>Description: <span>{{description}}</span></p>
      </div>
    {{/each}}
  </div>
{{else}}
  <p>No products found</p>
{{/if}}